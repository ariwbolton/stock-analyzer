// Author: Ari Bolton

#include "Day.h"

using namespace std;

Day::Day() {
    // no content
}

Day::Day(float c, float h, float l, float o, int v) : open(o), close(c), high(h),
    low(l), vol(v) {
	// no content
}
